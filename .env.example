# Discord Bot Token
DISCORD_TOKEN=your_bot_token_here

# Optional: Default crafter role ID (can be set per-guild via /set-crafter-role)
# DEFAULT_CRAFTER_ROLE_ID=

# Optional: Database path (defaults to ./requisitions.db)
# DATABASE_PATH=./requisitions.db
